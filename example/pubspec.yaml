name: example
description: A new Flutter project.

version: 1.0.0+1

publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_trimmer:
    path: ../

  video_player: ^2.7.2
  file_picker: ^5.5.0

dev_dependencies:
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
